const Input = ({typeProp, placeholderProp}) => {
    return (
        <>
            <input type={typeProp} placeholder={placeholderProp} className="form-control-md"/>
        </>
    )
}

export default Input;